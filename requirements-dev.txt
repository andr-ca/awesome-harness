# Pinned development/CI tools only. Install the hash-locked runtime packages
# first, as documented in CONTRIBUTING.md; keeping the resolver invocations
# separate prevents pip's global require-hashes mode from rejecting these
# pinned development tools. Bump deliberately and verify the full suite.
pytest==9.1.1
pytest-cov==7.1.0
hypothesis==6.156.6
setuptools==83.0.0
jsonschema==4.26.0
ruff==0.15.8
mypy==1.19.1
types-PyYAML==6.0.12.20260518
types-jsonschema==4.26.0.20260518
