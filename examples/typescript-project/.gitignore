# Pre-existing, realistic TypeScript/Node .gitignore — used to verify
# harness-link.sh's merge is additive and never disturbs entries already
# here (see verify.sh).
node_modules/
dist/
*.tsbuildinfo
