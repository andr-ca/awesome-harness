// Trivial placeholder — this fixture exists to verify harness integration
// against a realistic TypeScript project layout, not to demonstrate
// application code.

export function main(): number {
  console.log("typescript-project fixture");
  return 0;
}
